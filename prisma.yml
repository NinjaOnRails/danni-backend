endpoint: https://eu1.prisma.sh/danni/dev/dev
# endpoint: ${env:PRISMA_ENDPOINT_PROD}
secret: 'wC_!F&?rWj0#Q6wP/n-1'
# secret: ${env:PRISMA_SECRET_PROD}
datamodel: datamodel.prisma
hooks:
  post-deploy:
    - graphql get-schema -p prisma
